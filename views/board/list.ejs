<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
</head>
<body>
<h1><%= title %></h1>

<p>Welcome to <%= title %></p>
<a href="/board/write">글쓰기</a>

<table width="600" border="1">
    <tr>
        <th width="40">번호</th>
        <th width="200">제목</th>
        <th width="100">작성자</th>
        <th width="160">날짜</th>
        <th width="40">조회</th>
    </tr>
    <%
    for(var i = 0, len = data.length; i < len, item = data[i]; i++){
    %>
    <tr>
        <th><%= max - i %></th>
        <th><a href="/board/read/<%=page%>/<%=item.num%>"><%= item.title %></a></th>
        <th><%= item.id %></th>
        <th><%= item.regdate %></th>
        <th><%= item.hit %></th>
    </tr>
    <%
    }
    %>

</table>

<div style="width:46em;text-align:center;padding-top:2px">
    <% if(page > pageSize){ %>
    [ <a style="text-decoration:none" href="/board/list/1">◀◀</a> ]
    [ <a style="text-decoration:none" href="/board/list/<%= startPage - 1 %>">◀</a> ]
    <% }else{ %>
    [ <span style="color:gray"> ◀◀ </span> ]
    [ <span style="color:gray"> ◀ </span> ]
    <% } %>
    &nbsp;&nbsp;
    <% for (var i = startPage; i < endPage; i++) { %>
    <% if(i == page){ %>
    [ <%= i %> ]
    <% }else{ %>
    [ <a href="/board/list/<%= i %>"><%= i %></a> ]
    <% } %>
    <% } %>
    <% /* for end */ %>

    &nbsp;&nbsp;
    <% if (endPage < totalPage) { %>
    [ <a style="text-decoration:none" href="/board/list/<%= endPage + 1 %>">▶</a> ]
    [ <a style="text-decoration:none" href="/board/list/<%= totalPage %>">▶▶</a> ]
    <% }else{ %>
    [ <span style="color:gray"> ▶ </span> ]
    [ <span style="color:gray"> ▶▶ </span> ]

    <% } %>

</div>


</body>
</html>
